        <div id="leftWindow">
{{#unless customOrMealDetail}}
            <ul class="nav nav-tabs" id="myTab" role="tablist">
           
                <li class="nav-item" role="presentation">

                    <button class="nav-link link-success" id="favorites-tab" data-bs-toggle="tab"
                        data-bs-target="#favorites" type="button" role="tab" aria-controls="favorites"
                        aria-selected="false">Favorite Foods</button>
                </li>

                <li class="nav-item" role="presentation">
                    <button class="nav-link link-success" id="meals-tab" data-bs-toggle="tab" data-bs-target="#meals"
                        type="button" role="tab" aria-controls="meals" aria-selected="false">Saved Meals</button>
                </li>
            </ul>
            <div class="tab-content" id="myTabContent">
        

                <div class="tab-pane fade" id="favorites" role="tabpanel" aria-labelledby="favorites-tab">
                    <table class="table table-striped table-hover" id="foodTable">
                        <thead>
                            <tr>
                                <th scope="col">Food Name</th>
                                <th scope="col">Carbs</th>
                                <th scope="col"> Del.</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{> favorites}}
                        </tbody>
                    </table>
                </div>

                <div class="tab-pane fade" id="meals" role="tabpanel" aria-labelledby="meals-tab">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th scope="col">Meal Name</th>
                                <th scope="col">Carbs</th>
                                <th scope="col"> Del.</th>
                            </tr>
                        </thead>
                        <tbody>
                        {{#if usermeals}}
                          {{#each usermeals as |usermeal|}}
                            {{> meals}}
                          {{/each}}
                        {{else}}
                          <tr><td>There are no saved meals</td></tr>
                        {{/if}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
{{/unless}}
{{#if mealDetail}}
    <div>
        <h2>Meal detail</h2>
        <table>
            <thead>
                <tr>
                    <th>col 1</th>
                    <th>col 2</th>
                    <th>col 3</th>
                </tr>
            </thead>
            <tbody>
                {{> mealdetail}}
            </tbody>
        </table>
    </div>
{{/if}}
{{#if customFood}}
    <div>
        <h2>Custom food</h2>
        <form class="form signup-form">
            <div class="form-group">
                <label for="username-signup">Food name:</label>
                <input class="form-input" type="text" id="food-name" />
            </div>
            <div class="form-group">
                <label for="email-signup">Carb#:</label>
                <input class="form-input" type="number" id="carb-amount" />
            </div>
            <div class="form-group">
                <button class="btn btn-primary" type="submit">Submit</button>
            </div>
        </form>
    </div>
{{/if}}
        <div id="mealName">
            <div class="input-group mb-3">
                <input type="text" class="form-control" placeholder="Enter meal name" aria-label="Recipient's username"
                    aria-describedby="button-addon2">
                <button class="btn btn-outline-success" type="button" id="button-addon2">💾</button>
            </div>
        </div>

        <div id="rightWindow">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th scope="col">Food Name</th>
                        <th scope="col">Carbs (total)</th>
                        <th scope="col">Fav.</th>
                        <th scope="col"> Del.</th>
                    </tr>
                </thead>
                <tbody>
                    {{> meal }}
                </tbody>
            </table>
        </div>
{{!-- {{/unless}} --}}

{{!-- {{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}} --}}

<script src="/js/currentmeal.js"></script>